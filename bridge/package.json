{
  "name": "chess-bridge",
  "version": "1.0.0",
  "private": true,
  "description": "Bridges the compiled chess engine to chess.com computer games",
  "scripts": {
    "start": "ts-node --transpile-only src/index.ts",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "chess.js": "^1.0.0",
    "dotenv": "^16.3.1",
    "playwright": "^1.40.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.3.2"
  }
}
